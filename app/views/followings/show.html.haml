%h2 People I'm Following
%ul.attendees
  - current_attendee.following.each do |a|
    %li= link_to image_tag(a.gravatar_url, size: "80x80", title: a.name), attendee_path(a)
%ul= render partial: "event", collection: events